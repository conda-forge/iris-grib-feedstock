{% set python_min = "3.12" %}
{% set version = "0.23.0" %}


package:
  name: iris-grib
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/i/iris_grib/iris_grib-{{ version }}.tar.gz
  sha256: 15d804469e01261cfee3a8e046f979002d1015ada81d972f22d7b1e18272e3c2

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools >=77.0.3
    - setuptools-scm >=8
  run:
    - python >={{ python_min }}
    - iris >=3.10
    - python-eccodes >=1.6.1
    - eccodes >=2.33

test:
  imports:
    - iris_grib
  requires:
    - python {{ python_min }}

about:
  home: https://github.com/SciTools/iris-grib
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: Iris GRIB interface
  doc_url: https://iris-grib.readthedocs.io
  dev_url: https://github.com/SciTools/iris-grib

extra:
  recipe-maintainers:
    - bjlittle
    - lbdreyer
    - ocefpaf
    - pp-mo
    - trexfeathers
